<!DOCTYPE html>
<html>
  <head>
    <title>Postulaciones</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
<!--
    <link rel="stylesheet" type="text/css" media="screen, projection" href="http://www.artes.uchile.cl/u/framework/skeletons/artes/css/layout.css">
    <link rel="stylesheet" type="text/css" media="all" href="http://www.artes.uchile.cl/u/framework/skins/artes/css/screen.css">
-->
  </head>
  <body>

    <div class='container'>
      <div class='col-md-10 col-sm-10 col-md-offset-1 col-sm-offset-1'>
        <div class='col-md-12 text-center'>
          <img src='/assets/logo-negro-20.png' width='591px' height='300px'/>
        </div>
        
        <% if current_user%>
        <div class='user-block col-md-12'>
          <div class='col-md-3'>
            <%= "Tipo de usuario: #{current_user.roles.first.try(:name)}" %>
          </div>
          <div class='col-md-3'>
            <a class='btn btn-default' href='/postulations'>Ir a menu de postulaciones</a>
          </div>
          <% if !current_user.has_role?(:descarga) %>
          <div class='col-md-4'>
            <a class='btn btn-default' href='/info_requests'>Ir a menu de Solicitud de información</a>
          </div>
          <% end %>
          <div class='col-md-1'>
            <a href='/logout'>Salir</a>
          </div>
        </div>
        <% end %>
        <%= yield if !current_user %>
      </div>
    </div>
    <% if current_user %>
    <div class='container-fluid'>
      <div class='col-md-12'>
        <%= yield %>
      </div>
    </div>
    <% end %>
  </body>
</html>
